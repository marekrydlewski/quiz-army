<UserControl x:Class="RydlewskiJablonski.Quiz.UI.Menu.AddQuestion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RydlewskiJablonski.Quiz.UI.Menu"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding TestViewModel.Name}"></TextBlock>
        <TextBox Text="{Binding QuestionViewModel.Text, Mode=TwoWay}"></TextBox>
        <Button Name="NextQuestionButton" Content="Next question" Click="NextQuestionButton_Click"></Button>
    </StackPanel>
</UserControl>
